# HeatGuard — environment template. COPY to `.env` and fill in locally.
# `.env` is git-ignored; never commit a real key. The core demo needs NO key.
#
# Only required if you enable the OPTIONAL AI worker-briefing feature.
# Get a fresh key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
