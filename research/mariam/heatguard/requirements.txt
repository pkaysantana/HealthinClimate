# Pinned runtime + interface deps for the hackathon. `pip install -r requirements.txt`
# then `pip install -e .` to get the `heatguard` CLI.
pythermalcomfort==4.0.1
thermofeel==2.2.0
numpy==1.26.4
httpx>=0.24
# interfaces
fastapi>=0.110
uvicorn[standard]>=0.27
streamlit>=1.30
pandas>=2.0
altair>=5.0
# dev
pytest>=7
pytest-cov
